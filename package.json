{
  "name": "meeting-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix mobile\" \"npm run dev --prefix web\"",
    "dev:web": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix web\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:mobile": "npm run start --prefix mobile",
    "backend": "npm run dev --prefix backend",
    "mobile": "npm run start --prefix mobile",
    "web": "npm run dev --prefix web",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
